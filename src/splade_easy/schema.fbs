// src/splade_easy/schema.fbs
namespace SpladeEasy;

table KeyValue {
  key: string;
  value: string;
}

table Document {
  doc_id: string (required);
  text: [ubyte];
  metadata: [KeyValue];
  token_ids: [uint] (required);
  weights: [float] (required);
}

root_type Document;
